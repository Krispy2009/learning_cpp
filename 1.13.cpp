#include <iostream>

int main()
{
    int sum = 0;
    for(int val=50; val <=100; ++val)
    {
        sum += val;
    }
    std::cout << "The sum of range(50,101) is: " << sum << std::endl;
    std::cout << "And now we shall count down: " << std::endl;
    
    for(int val=10; val >= 0; val--w)
    {
        std::cout << val << std::endl;
    }
    return 0;
}