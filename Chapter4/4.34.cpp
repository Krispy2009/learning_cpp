#include <iostream>

/*(a) if(fval)

	if fval is 0 then condition is false, else fval is converted to 1 and
	condition is true.

*/

/*(b) dval = fval + ival

	ival is converted to float and the result is converted to double.

*/

/*(c) dval + ival * cval

	cval is converted to integer and the result is converted to double.

*/
